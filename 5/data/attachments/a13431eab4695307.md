# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Login Tests @regression >> Should display error message on incorrect email or password
- Location: tests/login.spec.ts:9:9

# Error details

```
TimeoutError: locator.waitFor: Timeout 15000ms exceeded.
Call log:
  - waiting for locator('[data-qa="login-email"]') to be visible
    2 × waiting for" https://automationexercise.com/login" navigation to finish...
      - navigated to "https://automationexercise.com/login"

```

# Page snapshot

```yaml
- generic [ref=e4]: Please wait while your request is being verified...
```

# Test source

```ts
  1  | import { Page, Locator } from "@playwright/test";
  2  | 
  3  | export class BasePage {
  4  |     constructor(protected page: Page) { }
  5  | 
  6  |     // Hàm điều hướng chung
  7  |     async navigateTo(url: string): Promise<void> {
  8  |         await this.page.goto(url, { waitUntil: "domcontentloaded" });
  9  |     }
  10 | 
  11 |     // Hàm click an toàn kèm cơ chế đợi phần tử sẵn sàng hiển thị
  12 |     async clickOn(selector: string | Locator): Promise<void> {
  13 |         const element = typeof selector === "string" ? this.page.locator(selector) : selector;
  14 |         await element.waitFor({ state: "visible" });
  15 |         await element.click();
  16 |     }
  17 | 
  18 |     // Hàm điền dữ liệu an toàn vào các ô nhập liệu
  19 |     async typeInto(selector: string | Locator, text: string): Promise<void> {
  20 |         const element = typeof selector === "string" ? this.page.locator(selector) : selector;
> 21 |         await element.waitFor({ state: "visible" });
     |                       ^ TimeoutError: locator.waitFor: Timeout 15000ms exceeded.
  22 |         await element.fill(text);
  23 |     }
  24 | 
  25 |     // Lấy nội dung text của một phần tử giao diện
  26 |     async getElementText(selector: string | Locator): Promise<string> {
  27 |         const element = typeof selector === "string" ? this.page.locator(selector) : selector;
  28 |         await element.waitFor({ state: "visible" });
  29 |         return (await element.textContent())?.trim() || "";
  30 |     }
  31 | }
```